name: Remove approvals when PR diff changes

on:
  pull_request:
    types: [ opened, edited, synchronize ]

jobs:
  waitaminute:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          target-branch: test/pr-6
          target-branch-filter: foo.*bar
